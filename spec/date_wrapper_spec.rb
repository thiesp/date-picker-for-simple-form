require "spec_helper"

RSpec.describe DateWrapper do
  it "has a version number" do
    expect(DateWrapper::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
